interface I{{ pascalCase name }} {
  
}

const {{ pascalCase name }} = ({

}: I{{ pascalCase name }} ) => {
  return (
    <div>
      Hello World
    </div>
  )
}

export default {{ pascalCase name }}